# MiniRAG 示例文档

这是一篇用于验证文档解析模块的 Markdown 示例。

## 为什么需要 RAG

大模型的知识是训练截止时点的快照，无法覆盖私有或新出现的资料。
RAG（检索增强生成）通过外挂检索把相关内容注入上下文，让模型基于
给定资料作答，并给出引用来源。

## 解析 → 分块 → 向量化 → 检索

全链路的第一步是解析：把原始文本读成结构化的文档对象，之后的分块、
向量化、检索都建立在干净的输入之上。解析如果做得草率，后面所有环节
都会建立在脏数据上。
