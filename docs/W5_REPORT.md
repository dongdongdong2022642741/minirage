# W5 评测报告：393 条 NoMIRACL zh x 五路召回

## 运行说明
- 命令：`python eval_retrieval.py`（重跑零 API，query 嵌入已缓存）
- 语料：31826 篇 passage（全量）；qrels 中 13 条 doc_id 不在库，已剔除；393 个 query 全部可评（0 个因无相关 doc 被排除）
- 参数：RAW_K=10（每路候选）、RRF_K=60（RRF 常数）、alpha=0.5（加权和），均为 W4 默认，本轮未调参

## 总表（393 query 平均）

| 方法 | recall@5 | recall@10 | MRR@10 |
|---|---:|---:|---:|
| bm25 | 0.3597 | 0.5581 | 0.3572 |
| vector | 0.7785 | 0.9358 | 0.7684 |
| rrf | 0.6469 | 0.8835 | 0.6225 |
| weighted | 0.7209 | 0.9013 | 0.6681 |
| rrf+rerank | 0.7209 | 0.8835 | 0.6447 |

## 三行结论

### a) BM25 vs 向量：向量全面胜出（W3 遗留问题）
向量 recall@10 0.9358 vs BM25 0.5581（+0.3777），recall@5 +0.4188，MRR@10 +0.4112。BM25 仅在 20 个 query（5.1%）上 recall@10 更优。

### b) RRF 平均低于较优单路（W4 验收在全部 query 上 FAIL）
RRF recall@10 0.8835 vs 向量 0.9358（-0.0523）。逐 query：输 66 个、平 319 个、赢 8 个，输的 query 平均亏 0.4599。加权和 0.9013（-0.0345）也未反超。rerank 把 recall@5 从 0.6469 提到 0.7209（+0.0740，88 个 query 受益，另有 37 个下降、268 个持平），recall@10 不变——集合不变只修排序。输入空间无碍（向量 recall@10 已 0.9358），损失来自 RRF 排名平权磨平分数断层，属方法问题，调参方向见下节。

### c) 融合赢输分型：关键词型 vs 语义型
按"单路谁 recall@10 更优"分组：BM25 优势（关键词型）20 个，向量优势（语义型）221 个，平局 152 个。

| 组 | n | bm25 | vector | rrf | weighted | 含数字 query 占比 |
|---|---:|---:|---:|---:|---:|---:|
| 关键词型 | 20 | 0.9214 | 0.5117 | 0.7982 | 0.7607 | 0.0% |
| 语义型 | 221 | 0.2570 | 0.9658 | 0.8443 | 0.8793 | 1.8% |
| 平局 | 152 | 0.9480 | 0.9480 | 0.9519 | 0.9518 | 1.3% |

RRF 输给较优单路的 66 个 query 集中在语义型（RRF recall@10 0.8443 < 向量 0.9658）；关键词型组融合表现：RRF 0.7982 vs BM25 0.9214 vs 向量 0.5117。

## 附：未调参声明与调参方向（问题 F 归因）
- 结论 b 的失败发生在输入空间充足的条件下（向量 recall@10=0.9358），归因于 RRF 机制（排名平权），非 RAW_K 太小。
- 未做参数扫描：RRF_K 调小（如 1~20）可削弱双榜加成、alpha 调大可向向量倾斜，均为候选方向；若调参须在本次全量 393 条上选一次并重新出表，不得用 demo 3 条或本表反推。
